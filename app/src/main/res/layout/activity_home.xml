<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="id.go.kemdikbud.pkpberbasiszonasi.HomeActivity">

    <FrameLayout
        android:id="@+id/frame_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

    </FrameLayout>


    <android.support.design.widget.BottomNavigationView
        android:id="@+id/navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@drawable/border_top_shadow"
        android:foreground="?attr/selectableItemBackground"
        app:itemBackground="@drawable/border_top_shadow"
        app:itemIconTint="@drawable/botnav_selector_color"
        app:itemTextColor="@drawable/botnav_selector_color"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/navigation_home" />

</android.support.design.widget.CoordinatorLayout>